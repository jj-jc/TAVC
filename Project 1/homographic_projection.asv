function [projected_I] = homographic_projection(H, I)

for x=1:size(I,2)
    for y=1:size(I,1)
        pos = [x,y,1]';
        pos_t = H*pos;
        post_t(1,1) = post_t(1,1) + 100;
        post_t(2,1) = post_t(2,1) + 100;
        projected_I(pos_t(1,1), pos_t(2,1), :)=I(x,y,:);
    end
end
end
